<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.cooks.dao.BoardRecipeDao">

	<select id="getArticleCount" resultType="Integer">
			select count(*) from RECIPE_BOARD
	</select>
	
	<select id="getArticles" parameterType="HashMap" resultType="recipe">
		select * 
		from (select rownum rm,human.* from 
		     (select RECIPE_NUM, ID, RECIPE_TITLE, RECIPE_HIT, RECIPE_DATE, RECIPE_LIKE 
		     from RECIPE_BOARD order by RECIPE_NUM) human) 		
		where rm between #{startRow} and #{endRow}
	</select>
	
</mapper>